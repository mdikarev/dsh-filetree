# i18n (EN/RU) + Basic Accessibility (L1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

**Goal:** Add plugin-local English-default/Russian-preserved i18n and L1 keyboard/semantic accessibility to the file-manager panel.

**Architecture:** A pure, typed l10n module (en/ru dictionaries, locale detection, tiny subscriber store) consumed through a React hook; components swap hard-coded Russian literals for t(key). Accessibility adds tree semantics (role tree/treeitem, aria-expanded, aria-level), row keyboard handling with a pure navigation helper, toggle-button semantics on the handle, preview dialog role + Escape close, and focus-visible styling — mouse/drag flows untouched.

**Tech Stack:** TypeScript, React 18 (host), node:test + tsx. No new runtime dependencies.

**Spec:** docs/superpowers/specs/2026-09-03-i18n-a11y-design.md

## Global Constraints

- Default UI language is English; Russian auto-selected when navigator language is ru; manual override via localStorage key fm-locale (values en|ru).
- en dictionary is the source of truth; ru must typecheck as Record<L10nKey, string> (full-key completeness).
- l10n.ts must be import-safe in Node tests (no top-level window/navigator/localStorage access); React lives only in the hook file src/use-l10n.ts.
- Existing mouse flows (click, drag-to-composer, hover tooltip, live refresh, git badges) and all host contracts/HTTP API unchanged.
- A11y depth L1 only: roles + keyboard + dialog/Esc + focus-visible. No aria-selected/focus-return/live-regions (L2/L3).
- Security/containment invariants untouched; strings only, no server changes.
- Gate before every commit: npm run typecheck && npm test && npm run build (currently 304 tests).

## File Structure

- Create: src/l10n.ts (dictionaries, detectLocale, getMessage, locale store, t())
- Create: src/use-l10n.ts (React hook wrapping the store)
- Create: test/l10n.test.ts (dictionary completeness, detection, locale store)
- Create: src/tree-nav.ts (pure keyboard-navigation helper treeNavStep)
- Modify: src/ToggleTab.tsx (strings + button semantics)
- Modify: src/Tree.tsx (strings, tree roles/aria, level prop, keyboard, container handler)
- Modify: src/Panel.tsx (strings, tree aria-label, preview dialog/Esc)
- Create: test/tree-nav.test.ts
- Modify: src/styles.ts (focus-visible)
- Modify: CHANGELOG.md, docs/superpowers/plans/2026-09-02-maturity-roadmap.md (final task)

---

### Task 1: l10n module + React hook + unit tests

**Files:**
- Create: src/l10n.ts
- Create: src/use-l10n.ts
- Test: test/l10n.test.ts

**Interfaces:**
- Consumes: nothing.
- Produces (used by Tasks 2-3):

    export type Locale = "en" | "ru";
    export const LOCALE_STORAGE_KEY = "fm-locale";
    export type L10nKey = keyof typeof en; // keys listed below
    export function detectLocale(navigatorLanguage: string, stored: string | null): Locale;
    export function getMessage(locale: Locale, key: L10nKey): string;
    export function getLocale(): Locale;
    export function setLocale(locale: Locale): void; // writes localStorage fm-locale + notifies
    export function subscribeLocale(listener: () => void): () => void;
    export function attachBrowserLocaleSync(): void; // window storage event -> live cross-tab switch
    export function t(key: L10nKey): string; // getMessage(getLocale(), key)

    // use-l10n.ts
    export function useL10n(): { locale: Locale; t: (key: L10nKey) => string };

    L10nKey values: filesFallback, refresh, close, openFiles, closePanel, emptyFolder, loading, errorPrefix, retry, noWorkspace, liveFallback, markdownMode, sourceMode, renderedMode, fileChanged, update, keepCurrent, previewUnavailablePrefix, localImagesUnavailable, fileTruncated.

- [ ] **Step 1: Write the failing tests**

    Create test/l10n.test.ts:

        import { describe, it } from "node:test";
        import assert from "node:assert";
        import {
          detectLocale,
          getMessage,
          getLocale,
          setLocale,
          subscribeLocale,
          type Locale,
        } from "../src/l10n.js";

        // Import both dictionaries indirectly through getMessage: en is
        // default so en keys are the source; completeness of ru is enforced
        // by the Record<L10nKey, string> type in l10n.ts (compile-time).

        describe("detectLocale", () => {
          it("prefers an explicit stored locale", () => {
            assert.strictEqual(detectLocale("en-US", "ru"), "ru");
            assert.strictEqual(detectLocale("ru", "en"), "en");
          });
          it("falls back to en for unknown navigator languages", () => {
            assert.strictEqual(detectLocale("fr-FR", null), "en");
            assert.strictEqual(detectLocale("", null), "en");
          });
          it("selects ru for ru navigator languages", () => {
            assert.strictEqual(detectLocale("ru-RU", null), "ru");
            assert.strictEqual(detectLocale("RU", null), "ru");
          });
          it("selects en for en navigator languages", () => {
            assert.strictEqual(detectLocale("en-GB", null), "en");
          });
        });

        describe("getMessage", () => {
          it("returns the en string by default", () => {
            assert.strictEqual(getMessage("en", "refresh"), "Refresh");
            assert.strictEqual(getMessage("en", "emptyFolder"), "Empty folder");
          });
          it("returns the ru string for ru", () => {
            assert.strictEqual(getMessage("ru", "refresh"), "Обновить");
            assert.strictEqual(getMessage("ru", "emptyFolder"), "Пустая папка");
          });
        });

        describe("locale store", () => {
          it("defaults to en and notifies subscribers on setLocale", () => {
            assert.strictEqual(getLocale(), "en");
            let seen: Locale | null = null;
            const off = subscribeLocale(() => { seen = getLocale(); });
            setLocale("ru");
            assert.strictEqual(seen, "ru");
            assert.strictEqual(getLocale(), "ru");
            off();
            setLocale("en");
            assert.strictEqual(getLocale(), "en");
          });
        });

    (The store tests must not depend on a real browser: in Node the module keeps a plain module-level current locale initialized to en. If writing to localStorage would throw in Node, setLocale must swallow the error — covered by implementation.)

- [ ] **Step 2: Run the tests and confirm they fail**

    Run: npx tsx --test test/l10n.test.ts
    Expected: FAIL — module ../src/l10n.js does not exist.

- [ ] **Step 3: Implement src/l10n.ts and src/use-l10n.ts**

    src/l10n.ts (complete file):

        // src/l10n.ts
        // Plugin-local minimal i18n: en is the default and the source of truth;
        // ru is preserved and auto-selected for ru-locale browsers. The DSH host
        // ships only zh/en locales, so the plugin cannot rely on it for Russian.
        // Pure module: no top-level browser access (Node-test safe). React lives
        // in src/use-l10n.ts.

        export type Locale = "en" | "ru";

        export const LOCALE_STORAGE_KEY = "fm-locale";

        const en = {
          filesFallback: "Files",
          refresh: "Refresh",
          close: "Close",
          openFiles: "Open files",
          closePanel: "Close panel",
          emptyFolder: "Empty folder",
          loading: "Loading…",
          errorPrefix: "Error: ",
          retry: "Retry",
          noWorkspace: "No workspace",
          liveFallback: "Auto-refresh: polling directories (SSE unavailable)",
          markdownMode: "Markdown mode",
          sourceMode: "Source",
          renderedMode: "Preview",
          fileChanged: "File changed on disk",
          update: "Update",
          keepCurrent: "Keep current version",
          previewUnavailablePrefix: "Preview unavailable: ",
          localImagesUnavailable: "Local images are unavailable in the preview.",
          fileTruncated: "File truncated at 5 MB; not all content is shown.",
        } as const;

        export type L10nKey = keyof typeof en;

        const ru: Record<L10nKey, string> = {
          filesFallback: "Файлы",
          refresh: "Обновить",
          close: "Закрыть",
          openFiles: "Открыть файлы",
          closePanel: "Закрыть панель",
          emptyFolder: "Пустая папка",
          loading: "Загрузка…",
          errorPrefix: "Ошибка: ",
          retry: "Повторить",
          noWorkspace: "Нет воркспейса",
          liveFallback: "Автообновление: опрос каталогов (SSE недоступен)",
          markdownMode: "Режим Markdown",
          sourceMode: "Исходник",
          renderedMode: "Предпросмотр",
          fileChanged: "Файл изменён на диске",
          update: "Обновить",
          keepCurrent: "Оставить текущую версию",
          previewUnavailablePrefix: "Предпросмотр недоступен: ",
          localImagesUnavailable: "Локальные изображения недоступны в предпросмотре.",
          fileTruncated: "Файл усечён до 5 МБ; показано не всё содержимое.",
        };

        export function detectLocale(navigatorLanguage: string, stored: string | null): Locale {
          if (stored === "en" || stored === "ru") return stored;
          const subtag = (navigatorLanguage ?? "").toLowerCase().split("-")[0] ?? "";
          return subtag === "ru" ? "ru" : "en";
        }

        export function getMessage(locale: Locale, key: L10nKey): string {
          return (locale === "ru" ? ru : en)[key];
        }

        function readStored(): string | null {
          try {
            if (typeof localStorage !== "undefined") return localStorage.getItem(LOCALE_STORAGE_KEY);
          } catch {
            // storage unavailable (private mode / tests)
          }
          return null;
        }

        function navigatorLanguage(): string {
          try {
            return typeof navigator !== "undefined" ? navigator.language : "";
          } catch {
            return "";
          }
        }

        let currentLocale: Locale = detectLocale(navigatorLanguage(), readStored());
        const listeners = new Set<() => void>();

        export function getLocale(): Locale {
          return currentLocale;
        }

        export function setLocale(locale: Locale): void {
          if (locale === currentLocale) return;
          currentLocale = locale;
          try {
            localStorage.setItem(LOCALE_STORAGE_KEY, locale);
          } catch {
            // storage unavailable
          }
          for (const listener of [...listeners]) listener();
        }

        export function subscribeLocale(listener: () => void): () => void {
          listeners.add(listener);
          return () => {
            listeners.delete(listener);
          };
        }

        export function t(key: L10nKey): string {
          return getMessage(currentLocale, key);
        }

        export function attachBrowserLocaleSync(): void {
          if (typeof window === "undefined") return;
          window.addEventListener("storage", (event) => {
            if (event.key !== LOCALE_STORAGE_KEY) return;
            const next = detectLocale(navigatorLanguage(), event.newValue);
            if (next !== currentLocale) {
              currentLocale = next;
              for (const listener of [...listeners]) listener();
            }
          });
        }

    src/use-l10n.ts (complete file):

        // src/use-l10n.ts
        import { useSyncExternalStore } from "react";
        import {
          getLocale,
          getMessage,
          subscribeLocale,
          type L10nKey,
          type Locale,
        } from "./l10n.js";

        export function useL10n(): { locale: Locale; t: (key: L10nKey) => string } {
          const locale = useSyncExternalStore(subscribeLocale, getLocale, getLocale);
          const t = (key: L10nKey): string => getMessage(locale, key);
          return { locale, t };
        }

- [ ] **Step 4: Run the tests and confirm they pass**

    Run: npx tsx --test test/l10n.test.ts
    Expected: PASS.

- [ ] **Step 5: Run the full gate**

    Run: npm run typecheck && npm test && npm run build
    Expected: green (304 existing + new l10n tests).

- [ ] **Step 6: Commit**

    git add src/l10n.ts src/use-l10n.ts test/l10n.test.ts
    git commit -m "feat: typed en/ru l10n module with live locale switching"

---

### Task 2: Replace hard-coded UI strings with t()

**Files:**
- Modify: src/Panel.tsx, src/Tree.tsx, src/ToggleTab.tsx
- Modify: src/client.tsx (call attachBrowserLocaleSync once in apply)

**Interfaces:**
- Consumes: Task 1 exports (useL10n, attachBrowserLocaleSync).
- Produces: no new interfaces (behavioral string swap).

- [ ] **Step 1: Wire attachBrowserLocaleSync in client.tsx**

    In src/client.tsx, import attachBrowserLocaleSync from "./l10n.js" and call it inside the existing apply(ctx) function body (after injectCss(CSS_STRING), before or after installDragDropListeners — order does not matter).

- [ ] **Step 2: Panel.tsx strings**

    Add to the imports: import { useL10n } from "./use-l10n.js";
    At the top of the Panel function body add: const { t } = useL10n();
    Replace these literals:

        - {rootName || "Файлы"}                  -> {rootName || t("filesFallback")}
        - title="Обновить"                        -> title={t("refresh")}
        - title="Закрыть"  (header close)        -> title={t("close")}
        - Загрузка… (loading, tree status)        -> {t("loading")}
        - Ошибка: {error}                          -> {t("errorPrefix")}{error}
        - button Повторить                          -> {t("retry")}
        - <div className="fm-empty">Нет воркспейса</div> -> <div className="fm-empty">{t("noWorkspace")}</div>
        - Автообновление: опрос каталогов (SSE недоступен) -> {t("liveFallback")}
        - aria-label="Режим Markdown"             -> aria-label={t("markdownMode")}
        - >Исходник</button>                       -> >{t("sourceMode")}</button>
        - >Предпросмотр</button>                   -> >{t("renderedMode")}</button>
        - title="Закрыть" (preview close)         -> title={t("close")}
        - Файл изменён на диске                    -> {t("fileChanged")}
        - >Обновить</button> (banner)              -> >{t("update")}</button>
        - >Оставить текущую версию</button>        -> >{t("keepCurrent")}</button>
        - {t("loading")} inside the preview-body spinner too (there are two Загрузка… instances)
        - Ошибка: {previewError}                   -> {t("errorPrefix")}{previewError}
        - Предпросмотр недоступен: {..error}       -> {t("previewUnavailablePrefix")}{previewPresentation.error}
        - Локальные изображения недоступны...      -> {t("localImagesUnavailable")}
        - Файл усечён до 5 МБ; показано не всё...  -> {t("fileTruncated")}

- [ ] **Step 3: Tree.tsx and ToggleTab.tsx strings**

    Tree.tsx: import { useL10n } from "./use-l10n.js"; inside the Tree component (forwardRef body) add const { t } = useL10n(); replace <div className="fm-empty">Пустая папка</div> with <div className="fm-empty">{t("emptyFolder")}</div>.

    ToggleTab.tsx: import { useL10n } from "./use-l10n.js"; in the component add const { t } = useL10n(); replace the title expression title={open ? "Закрыть панель" : "Открыть файлы"} with title={open ? t("closePanel") : t("openFiles")}.

- [ ] **Step 4: Gate and check for leftover Russian UI strings**

    Run: npm run typecheck && npm test && npm run build; then grep for remaining user-facing Cyrillic string literals in JSX/string literals (comments in styles.ts/tooltip.ts are fine): grep -rnE "[А-Яа-яЁё]" src/Panel.tsx src/Tree.tsx src/ToggleTab.tsx | grep -v "//" | grep -vE "t\\(|title=\{|aria-label=\{" ; expected: no user-facing hits (any hits that remain are code comments or already-wrapped expressions — double-check each).

- [ ] **Step 5: Commit**

    git add src/Panel.tsx src/Tree.tsx src/ToggleTab.tsx src/client.tsx
    git commit -m "feat: localize panel UI strings (en default, ru preserved)"

---

### Task 3: Basic accessibility (L1)

**Files:**
- Modify: src/Tree.tsx (roles/aria, level prop, keyboard)
- Modify: src/ToggleTab.tsx (button semantics)
- Modify: src/Panel.tsx (preview dialog role/aria-label, Escape close)
- Modify: src/styles.ts (focus-visible)
- Create: src/tree-nav.ts (treeNavStep helper)
- Test: test/tree-nav.test.ts

**Interfaces:**
- Consumes: nothing new from Tasks 1-2 (l10n already wired).
- Produces: exported pure helper treeNavStep (used by Tree.tsx keyboard handling):

    export type TreeNavKey = "ArrowUp" | "ArrowDown" | "Home" | "End";
    export function treeNavStep(current: number, visibleCount: number, key: TreeNavKey): number | null;

    Semantics: Home -> 0, End -> visibleCount-1, ArrowUp -> max(0, current-1), ArrowDown -> min(visibleCount-1, current+1), otherwise null. visibleCount <= 0 -> null. The helper lives in the new file src/tree-nav.ts; Tree.tsx imports it from "./tree-nav.js".

- [ ] **Step 1: Write the failing treeNavStep tests**

    Create test/tree-nav.test.ts importing treeNavStep from "../src/tree-nav.js":

        describe("treeNavStep", () => {
          it("moves one step and clamps at the ends", () => {
            assert.strictEqual(treeNavStep(2, 5, "ArrowDown"), 3);
            assert.strictEqual(treeNavStep(4, 5, "ArrowDown"), 4);
            assert.strictEqual(treeNavStep(0, 5, "ArrowUp"), 0);
            assert.strictEqual(treeNavStep(2, 5, "ArrowUp"), 1);
          });
          it("supports Home and End", () => {
            assert.strictEqual(treeNavStep(3, 7, "Home"), 0);
            assert.strictEqual(treeNavStep(0, 7, "End"), 6);
          });
          it("returns null for empty trees and unknown keys", () => {
            assert.strictEqual(treeNavStep(0, 0, "ArrowDown"), null);
            assert.strictEqual(treeNavStep(0, 3, "Enter" as any), null);
          });
        });

- [ ] **Step 2: Run the tests and confirm they fail**

    Run: npx tsx --test --test-name-pattern="treeNavStep" test/tree-nav.test.ts
    Expected: FAIL (function undefined).

- [ ] **Step 3: Implement treeNavStep in src/tree-nav.ts**

        export type TreeNavKey = "ArrowUp" | "ArrowDown" | "Home" | "End";

        export function treeNavStep(
          current: number,
          visibleCount: number,
          key: TreeNavKey
        ): number | null {
          if (visibleCount <= 0) return null;
          if (key === "Home") return 0;
          if (key === "End") return visibleCount - 1;
          if (key === "ArrowUp") return current > 0 ? current - 1 : 0;
          if (key === "ArrowDown") return current < visibleCount - 1 ? current + 1 : visibleCount - 1;
          return null;
        }

- [ ] **Step 4: Implement the a11y changes in the components**

    Tree.tsx:

    - Add props: TreeProps gains label: string (aria-label of the tree); TreeNodeProps gains level: number (root rows level 1). Pass level={level + 1} on recursive children; Tree maps entries with level={1}.
    - Container: replace <div className="fm-tree"> with <div className="fm-tree" role="tree" aria-label={label} onKeyDown={handleTreeKeyDown}>. Keep the fm-empty branch before it unchanged (it is not a tree).
    - Rows: add role="treeitem", aria-level={level}, tabIndex={0}, and for folders aria-expanded={expanded} (only set the attribute for isDir rows; files get role treeitem + aria-level + tabIndex only).
    - Row keyboard: add onKeyDown={handleRowKeyDown} to the row div:

        const handleRowKeyDown = (e: ReactKeyboardEvent<HTMLDivElement>): void => {
          const key = e.key;
          if (key === "Enter" || key === " ") {
            e.preventDefault();
            handleToggle();
            return;
          }
          if (isDir && (key === "ArrowRight" || key === "ArrowLeft")) {
            const wantsExpand = key === "ArrowRight";
            if ((wantsExpand && !expanded) || (!wantsExpand && expanded)) {
              e.preventDefault();
              handleToggle();
            }
          }
        };

      Import the keyboard event type as ReactKeyboardEvent alongside the existing MouseEvent type import. handleToggle is already in scope inside TreeNode.

    - Import treeNavStep and TreeNavKey in Tree.tsx (import { treeNavStep, type TreeNavKey } from "./tree-nav.js";).
    - Tree container keyboard handler (declared inside the Tree component, above the return):

        const handleTreeKeyDown = (e: ReactKeyboardEvent<HTMLDivElement>): void => {
          const key = e.key as TreeNavKey;
          if (key !== "ArrowUp" && key !== "ArrowDown" && key !== "Home" && key !== "End") return;
          const rows = Array.from(e.currentTarget.querySelectorAll<HTMLElement>(".fm-row"));
          if (rows.length === 0) return;
          const current = rows.indexOf(document.activeElement as HTMLElement);
          const next = treeNavStep(current, rows.length, key);
          if (next !== null) {
            e.preventDefault();
            rows[next].focus();
          }
        };

      (The DOM row set equals the visible tree because collapsed children are not rendered. Enter/Space/ArrowLeft/ArrowRight are handled at the row level and bubble up; the container only reacts to the four nav keys.)

    ToggleTab.tsx: add to the handle div: role="button", tabIndex={0}, aria-expanded={open}, and onKeyDown handling Enter/Space (preventDefault + onToggle).

    Panel.tsx:

    - Pass the tree label: <Tree label={rootName || t("filesFallback")} ... /> (t already in scope from Task 2).
    - Preview window div: add role="dialog" and aria-label={previewTitle}.
    - Escape close: add an effect (deps [previewOpen, handleClosePreview]):

        useEffect(() => {
          if (!previewOpen) return;
          const onKey = (event: KeyboardEvent): void => {
            if (event.key === "Escape") handleClosePreview();
          };
          window.addEventListener("keydown", onKey);
          return () => window.removeEventListener("keydown", onKey);
        }, [previewOpen, handleClosePreview]);

    styles.ts: append

        .fm-toggle:focus-visible,
        .fm-row:focus-visible,
        .fm-header-btn:focus-visible,
        .fm-preview-toggle button:focus-visible,
        .fm-preview-close:focus-visible,
        .fm-preview-changed-btn:focus-visible {
          outline: 2px solid var(--dsw-alias-brand-primary);
          outline-offset: 2px;
        }
        .fm-row:focus-visible {
          outline-offset: -2px;
        }

- [ ] **Step 5: Run the tests and confirm they pass**

    Run: npx tsx --test --test-name-pattern="treeNavStep" test/tree-nav.test.ts
    Expected: PASS.

- [ ] **Step 6: Run the full gate**

    Run: npm run typecheck && npm test && npm run build
    Expected: green. Also verify Tree receives its new required label prop everywhere it is used (single usage in Panel.tsx).

- [ ] **Step 7: Commit**

    git add src/Tree.tsx src/ToggleTab.tsx src/Panel.tsx src/styles.ts src/tree-nav.ts test/tree-nav.test.ts
    git commit -m "feat: basic tree accessibility (roles, keyboard nav, dialog Esc, focus styles)"

---

### Task 4: Documentation status and final gate

**Files:**
- Modify: CHANGELOG.md
- Modify: docs/superpowers/plans/2026-09-02-maturity-roadmap.md

**Interfaces:** Consumes Tasks 1-3 merged. Produces nothing code-facing.

- [ ] **Step 1: Update the roadmap**

    In docs/superpowers/plans/2026-09-02-maturity-roadmap.md mark the phase G row done (Status: done - 2026-09-03, plan docs/superpowers/plans/2026-09-03-i18n-a11y.md).

- [ ] **Step 2: Update the changelog**

    Under CHANGELOG.md Unreleased > Changed add:

        - UI is localized: English default with Russian preserved (auto-selected
          for ru-locale browsers; override via fm-locale)
        - Basic accessibility: tree semantics + keyboard navigation (arrows,
          Home/End, Enter/Space, ArrowLeft/Right on folders), preview dialog
          closes on Escape, toggle handle is a button, focus-visible styles

- [ ] **Step 3: Run the full gate once more**

    Run: npm run typecheck && npm test && npm run build
    Expected: all green.

- [ ] **Step 4: Commit**

    git add CHANGELOG.md docs/superpowers/plans/2026-09-02-maturity-roadmap.md
    git commit -m "docs: mark phase G (i18n + basic a11y) complete"
